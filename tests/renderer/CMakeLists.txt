# Renderer tests CMakeLists.txt

# Buffer tests
#add_ramo3d_test(buffer_tests 
#    buffer_tests.cpp
#)

# Shader tests
#add_ramo3d_test(shader_tests
#    shader_tests.cpp
#)

# Renderer API tests
#add_ramo3d_test(renderer_api_tests
#    renderer_api_tests.cpp
#)

# Backend-specific tests
#if(ENABLE_OPENGL)
#    add_ramo3d_test(opengl_tests
#        opengl/opengl_tests.cpp
#    )
#endif()

#if(ENABLE_SOFTWARE)
#    add_ramo3d_test(software_renderer_tests
#        software/software_renderer_tests.cpp
#    )
#endif()

# Examples CMakeLists.txt
cmake_minimum_required(VERSION 3.14)

# We'll add actual examples later as we develop the core functionality
message(STATUS "Examples directory - No examples configured yet")